# AGENTS.md

本仓库的代理协作说明位于 [AGENT.md](AGENT.md)。

某些工具默认读取 `AGENTS.md`，所以这里保留一个兼容入口。实际约定请以 `AGENT.md` 为准。

一句话提醒：FileSage 是证据驱动、懂用户偏好的智能文件整理高级顾问。AI、settings、CLI 和 GUI 都必须调用同一个 File Organization Runtime，不能绕过 runtime 做 best effort 文件操作。
